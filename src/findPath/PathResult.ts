export class PathResult {
    static NO_RELATIONSHIP: Number = 0; 	// the path does not cross this cell
    static ENDING_CELL: Number = 1; 		// the path ends in this cell
    static EXITING_CELL: Number = 2;

    constructor() {
    }
}