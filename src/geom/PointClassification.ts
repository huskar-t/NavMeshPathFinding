export class PointClassification {
    static ON_LINE: number = 0;		// The point is on, or very near, the line
    static LEFT_SIDE: number = 1;		// looking from endpoint A to B, the test point is on the left
    static RIGHT_SIDE: number = 2;


    // looking from endpoint A to B, the test point is on the right
    constructor() {

    }
}